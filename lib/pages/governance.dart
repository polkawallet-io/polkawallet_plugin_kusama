import 'package:flutter/cupertino.dart';

class Gov extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('gov page content'),
    );
  }
}
