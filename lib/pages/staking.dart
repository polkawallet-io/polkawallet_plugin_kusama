import 'package:flutter/cupertino.dart';

class Staking extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('staking page content'),
    );
  }
}
